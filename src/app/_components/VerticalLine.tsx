import {cn} from "@/app/_lib/utils/cn";

const VerticalLine = () => (
	<div className={cn(
		'inline-block', 'h-full', 'w-0.5', 'self-stretch', 'bg-primary-container'
	)}/>
)

export default VerticalLine